# Movie Suggester

## Run the script

Required Modules
```
pip install requests beautifulsoup4 
```
Run the script
```
python suggest.py
```

## Run using docker

Build the image
```
docker build -t movie-suggester .
```

Run the image
```
docker run -it movie-suggester
```
